# Next.js Foundations

## Local Development

Assume running commands in `cd path/to/project/` context.

### Setup

```bash
npm install react react-dom next
```

### Run

Visit `localhost:3000` after running:

```bash
npm run dev
```
